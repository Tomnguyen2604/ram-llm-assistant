# Core ML / HF
transformers>=4.57.1,<5
datasets>=2.11.0,<3
huggingface-hub>=0.18.0
accelerate>=0.26.0

# PyTorch: install separately using the official command for your CUDA (see note below)
torch>=2.0.0
torchvision>=0.16.0
torchaudio>=2.0.0

# Utilities and tokenizers
sentencepiece>=0.1.99
safetensors>=0.3.0
tqdm>=4.66.0
psutil>=5.9.0

# Fixes for Windows multiprocess / pickling issues
multiprocess==0.70.14
dill==0.3.6